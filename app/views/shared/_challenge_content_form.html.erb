<%= form_for chal_instance, url: {action: url_instance}, remote: true, authenticity_token: true do |f| %><%= f.hidden_field :id, :value => chal_instance.id %><%= f.text_area :content, :value => chal_instance.content, :class => "tinymce", :rows => 40, :cols => 120 %><%= f.submit 'Save', :class => "btn btn-success" %><% end %>