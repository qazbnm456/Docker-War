<%= form_for chal_instance, url: {action: url_instance}, remote: true do |f| %><%= f.hidden_field :id, :value => chal_instance.id %><%= f.text_field :content, :value => chal_instance.content %><%= f.submit 'Save', :class => "btn btn-success" %><% end %>