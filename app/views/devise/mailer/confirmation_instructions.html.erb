<p class="tp">Welcome <%= @email %>!</p>

<p class="tp">You can confirm your account email through the link below:</p>

<p class="tp"><%= link_to 'Confirm my account', confirmation_url(@resource, confirmation_token: @token) %></p>
